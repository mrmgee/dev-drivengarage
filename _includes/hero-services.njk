    <section class="hero-services">
        <div class="halftone-overlay"></div>
        <div class="sec-bkg-img">

        {% if featuredImage %}
          <img src="{{ rootPath }}{{featuredImage}}" alt="{{title}}" style="margin-top: -24%; object-position: 0px -440px;">
        {% endif %}

        </div>
        <div class="hero-body is-overlay">
            <div class="container">
                <h1 class="title">{{title}}</h1>
            </div>
        </div>
    </section>